version: '3.8'

services:
  clock:
    build: .
    container_name: clock-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
